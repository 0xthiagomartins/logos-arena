"""Application services layer.

This package contains functions that implement application-level use cases
for the backend (e.g. debate lifecycle), orchestrating auth, persistence
and LLM calls. HTTP details should be kept in the `api` package.
"""

